'use strict'

let userName = 'Максим';
let userSurname = 'Кунділовський';

if (userName.length > 4 && userSurname.length > 5) {

    alert(userName.length + userSurname.length);

} else {
    alert('УПС')
}